BOT_TOKEN=your-telegram-bot-token-here
MAX_FILE_SIZE_MB=50
DOWNLOADS_DIR=downloads
# Optional: only needed if ffmpeg is not on PATH. Point this at the folder
# containing ffmpeg.exe/ffprobe.exe (e.g. an extracted release zip).
# FFMPEG_LOCATION=C:\Program Files\ffmpeg-master-latest-win64-gpl-shared\bin
